# Runtime dependencies (pinned for stability)
fastapi
uvicorn[standard]
httpx
pyyaml
prometheus-client
starlette
pydantic>=2.5.0,<3.0.0

# Development dependencies (can be removed for production image)
pytest
pytest-asyncio
pytest-httpx
respx



django